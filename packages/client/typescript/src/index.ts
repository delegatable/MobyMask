
// Code generated by @open-rpc/generator DO NOT EDIT.
import { RequestManager, PostMessageWindowTransport, PostMessageIframeTransport, WebSocketTransport, HTTPTransport, Client, JSONRPCError } from "@open-rpc/client-js";
import _ from "lodash";
import { OpenrpcDocument as OpenRPC, MethodObject, ContentDescriptorObject } from "@open-rpc/meta-schema";
import { MethodCallValidator, MethodNotFoundError } from "@open-rpc/schema-utils-js";

export type Nonce = string;
export interface ReplayProtection {
  nonce?: Nonce;
  [k: string]: any;
}
export type SignedDelegation = string;
export type UnorderedSetOfSignedDelegationb1Sgec3B = SignedDelegation[];
export type To = string;
export type From = string;
export type GasLimit = string;
export interface Transaction {
  to?: To;
  from?: From;
  gasLimit?: GasLimit;
  [k: string]: any;
}
export interface ObjectOfTransactionUnorderedSetOfSignedDelegationb1Sgec3BQqYVpQKF {
  authority?: UnorderedSetOfSignedDelegationb1Sgec3B;
  transaction?: Transaction;
  [k: string]: any;
}
export type UnorderedSetOfObjectOfTransactionUnorderedSetOfSignedDelegationb1Sgec3BQqYVpQKF4EXHjMMR = ObjectOfTransactionUnorderedSetOfSignedDelegationb1Sgec3BQqYVpQKF[];
export interface Invocations {
  replayProtection?: ReplayProtection;
  batch?: UnorderedSetOfObjectOfTransactionUnorderedSetOfSignedDelegationb1Sgec3BQqYVpQKF4EXHjMMR;
  [k: string]: any;
}
export type BooleanVyG3AETh = boolean;
/**
 *
 * Generated! Represents an alias to any of the provided schemas
 *
 */
export type AnyOfInvocationsBooleanVyG3AETh = Invocations | BooleanVyG3AETh;
export type SubmitInvocations = (InvocationMessage: Invocations) => Promise<BooleanVyG3AETh>;

export interface Options {
  transport: {
    type: "websocket" | "http" | "https" | "postmessagewindow" | "postmessageiframe";
    host: string;
    port: number;
    path?: string;
    protocol?: string;
  },
}

export class MobyMaskAPI {
  public rpc: Client;
  public static openrpcDocument: OpenRPC = {"openrpc":"1.2.4","info":{"title":"MobyMaskAPI","version":"1.0.0"},"methods":[{"name":"submitInvocations","description":"Accepts invocations stores them","params":[{"name":"InvocationMessage","description":"An invocation you want the server to accept","schema":{"title":"Invocations","type":"object","properties":{"replayProtection":{"title":"ReplayProtection","type":"object","properties":{"nonce":{"title":"Nonce","type":"string","format":"HexNumber","pattern":"^0x[0-9a-f]*$"}}},"batch":{"type":"array","items":{"type":"object","properties":{"authority":{"type":"array","items":{"title":"SignedDelegation","type":"string","pattern":"^0x[0-9a-f]*$"},"title":"unorderedSetOf_SignedDelegationb1sgec3b"},"transaction":{"title":"Transaction","type":"object","properties":{"to":{"title":"to","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"from":{"title":"from","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"gasLimit":{"title":"Gas Limit","type":"string","format":"HexNumber","pattern":"^0x([1-9a-f]+[0-9a-f]*|0)$"}}}},"title":"objectOf_Transaction_unorderedSetOf_SignedDelegationb1sgec3b_qqYVpQKF"},"title":"unorderedSetOf_objectOf_Transaction_unorderedSetOf_SignedDelegationb1sgec3b_qqYVpQKF4EXHjMMR"}}}}],"result":{"name":"SubmitInvocationResult","description":"Returns `true` if successfull. Error on failure","schema":{"type":"boolean"}}}],"components":{"schemas":{"ReplayProtection":{"title":"ReplayProtection","type":"object","properties":{"nonce":{"title":"Nonce","type":"string","format":"HexNumber","pattern":"^0x[0-9a-f]*$"}}},"Address":{"title":"Address","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"Transaction":{"title":"Transaction","type":"object","properties":{"to":{"title":"to","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"from":{"title":"from","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"gasLimit":{"title":"Gas Limit","type":"string","format":"HexNumber","pattern":"^0x([1-9a-f]+[0-9a-f]*|0)$"}}},"SignedDelegation":{"title":"SignedDelegation","type":"string","pattern":"^0x[0-9a-f]*$"},"Invocation":{"type":"object","properties":{"authority":{"type":"array","items":{"title":"SignedDelegation","type":"string","pattern":"^0x[0-9a-f]*$"},"title":"unorderedSetOf_SignedDelegationb1sgec3b"},"transaction":{"title":"Transaction","type":"object","properties":{"to":{"title":"to","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"from":{"title":"from","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"gasLimit":{"title":"Gas Limit","type":"string","format":"HexNumber","pattern":"^0x([1-9a-f]+[0-9a-f]*|0)$"}}}},"title":"objectOf_Transaction_unorderedSetOf_SignedDelegationb1sgec3b_qqYVpQKF"},"Invocations":{"title":"Invocations","type":"object","properties":{"replayProtection":{"title":"ReplayProtection","type":"object","properties":{"nonce":{"title":"Nonce","type":"string","format":"HexNumber","pattern":"^0x[0-9a-f]*$"}}},"batch":{"type":"array","items":{"type":"object","properties":{"authority":{"type":"array","items":{"title":"SignedDelegation","type":"string","pattern":"^0x[0-9a-f]*$"},"title":"unorderedSetOf_SignedDelegationb1sgec3b"},"transaction":{"title":"Transaction","type":"object","properties":{"to":{"title":"to","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"from":{"title":"from","type":"string","pattern":"^0x[0-9,a-f,A-F]{40}$"},"gasLimit":{"title":"Gas Limit","type":"string","format":"HexNumber","pattern":"^0x([1-9a-f]+[0-9a-f]*|0)$"}}}},"title":"objectOf_Transaction_unorderedSetOf_SignedDelegationb1sgec3b_qqYVpQKF"},"title":"unorderedSetOf_objectOf_Transaction_unorderedSetOf_SignedDelegationb1sgec3b_qqYVpQKF4EXHjMMR"}}}}}} ;
  public transport: HTTPTransport | WebSocketTransport | PostMessageWindowTransport | PostMessageIframeTransport;
  private validator: MethodCallValidator;
  private timeout: number | undefined;

  constructor(options: Options) {

    if (options.transport === undefined || options.transport.type === undefined) {
      throw new Error("Invalid constructor params");
    }
    const {type, host, port, protocol} = options.transport;
    let path = options.transport.path || "";
    if(path && path[0] !== "/") {
        path = "/" + path;
    }
    switch (type) {
      case 'http':
      case 'https':
        this.transport = new HTTPTransport((protocol || type) + "://" + host + ":" + port + path);
        break;
      case 'websocket':
        this.transport = new WebSocketTransport((protocol || "ws://") + host + ":" + port + path);
        break;
      case 'postmessageiframe':
        this.transport = new PostMessageIframeTransport(protocol + "://" + host + ":" + port + path);
        break;
      case 'postmessagewindow':
        this.transport = new PostMessageWindowTransport(protocol + "://" + host + ":" + port + path);
        break;
      default:
        throw new Error("unsupported transport");
        break;
    }
    this.rpc = new Client(new RequestManager([this.transport]));
    this.validator = new MethodCallValidator(MobyMaskAPI.openrpcDocument);
  }
  /**
   * Adds a JSONRPC notification handler to handle receiving notifications.
   * @example
   * myClient.onNotification((data)=>console.log(data));
   */
  public onNotification(callback: (data: any) => void) {
    this.rpc.onNotification(callback);
  }

  /**
   * Adds an optional JSONRPCError handler to handle receiving errors that cannot be resolved to a specific request
   * @example
   * myClient.onError((err: JSONRPCError)=>console.log(err.message));
   */
  public onError(callback: (data: JSONRPCError) => void) {
     this.rpc.onError(callback);
  }

  /**
   * Sets a default timeout in ms for all requests excluding notifications.
   * @example
   * // 20s timeout
   * myClient.setDefaultTimeout(20000);
   * // Removes timeout from request
   * myClient.setDefaultTimeout(undefined);
   */
   public setDefaultTimeout(ms?: number) {
    this.timeout = ms;
  }

  /**
   * Initiates [[MobyMaskAPI.startBatch]] in order to build a batch call.
   *
   * Subsequent calls to [[MobyMaskAPI.request]] will be added to the batch.
   * Once [[MobyMaskAPI.stopBatch]] is called, the promises for the [[MobyMaskAPI.request]]
   * will then be resolved.  If there is already a batch in progress this method is a noop.
   *
   * @example
   * myClient.startBatch();
   * myClient.foo().then(() => console.log("foobar"))
   * myClient.bar().then(() => console.log("foobarbaz"))
   * myClient.stopBatch();
   */
  public startBatch(): void {
    return this.rpc.startBatch();
  }

  /**
   * Initiates [[Client.stopBatch]] in order to finalize and send the batch to the underlying transport.
   *
   * stopBatch will send the [[MobyMaskAPI]] calls made since the last [[MobyMaskAPI.startBatch]] call. For
   * that reason, [[MobyMaskAPI.startBatch]] MUST be called before [[MobyMaskAPI.stopBatch]].
   *
   * @example
   * myClient.startBatch();
   * myClient.foo().then(() => console.log("foobar"))
   * myClient.bar().then(() => console.log("foobarbaz"))
   * myClient.stopBatch();
   */
  public stopBatch(): void {
    return this.rpc.stopBatch();
  }

  private request(methodName: string, params: any[]): Promise<any> {
    const methodObject = _.find((MobyMaskAPI.openrpcDocument.methods as MethodObject[]), ({name}) => name === methodName) as MethodObject;
    const notification = methodObject.result ? false : true;
    const openRpcMethodValidationErrors = this.validator.validate(methodName, params);
    if ( openRpcMethodValidationErrors instanceof MethodNotFoundError || openRpcMethodValidationErrors.length > 0) {
      return Promise.reject(openRpcMethodValidationErrors);
    }

    let rpcParams;
    if (methodObject.paramStructure && methodObject.paramStructure === "by-name") {
      rpcParams = _.zipObject(_.map(methodObject.params, "name"), params);
    } else {
      rpcParams = params;
    }
    if (notification) {
      return this.rpc.notify({method: methodName, params: rpcParams});
    }
    return this.rpc.request({method: methodName, params: rpcParams}, this.timeout);
  }

  
  /**
   * 
   */
  // tslint:disable-next-line:max-line-length
  public submitInvocations: SubmitInvocations = (...params) => {
    return this.request("submitInvocations", params);
  }
  
}
export default MobyMaskAPI;
